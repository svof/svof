tempTimer(7.5, function () svo.echof("%s's <255,0,0>sileris%s coming up in two (2) seconds.", tostring(svo.rt_sname), svo.getDefaultColor()) end)
tempTimer(8.5, function () svo.echof("%s's <255,0,0>sileris%s coming up in one (1) second.", tostring(svo.rt_sname), svo.getDefaultColor()) end)
svo.rt_flayed = nil